# ─────────────────────────────────────────────
# Variables para docker-compose (producción)
# Copia este archivo como .env y ajusta los valores
# ─────────────────────────────────────────────

# ── MySQL ──────────────────────────────────────
MYSQL_ROOT_PASSWORD=CambiaEsteRootPass2024!
MYSQL_DATABASE=sistema_pedidos
MYSQL_USER=sispedidos
MYSQL_PASSWORD=CambiaEstePass2024!

# ── Backend ────────────────────────────────────
# JWT_SECRET: mínimo 32 caracteres aleatorios
JWT_SECRET=c4mb1a_3st0_p0r_un4_cl4v3_muy_s3gur4_aqui
JWT_EXPIRES_IN=7d
# CORS: dominio del frontend (o * para cualquier origen en dev/staging)
CORS_ORIGIN=https://tudominio.com

# ── Frontend Vite ──────────────────────────────
# Al usar nginx proxy (/api/ → backend) dejar en /api
VITE_API_URL=/api

# ── Puertos expuestos (opcional, Dokploy maneja esto) ──
FRONTEND_PORT=80
BACKEND_PORT=3000
